#include <stdio.h>
/**
 * main - Main Function
 * @argc : contains the number of argument
 * @argv : contains the string of the argument
 *
 * Return: Always returns 0
 */

int main(int argc, char __attribute__((__unused__)) *argv[])
{
printf("%d\n", argc - 1);

return (0);
}
