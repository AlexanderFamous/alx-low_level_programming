#include <stdio.h>
/**
 * main - Main function
 * @argc : holds the argument count
 * @argv : holds the strings of the argument bin the compiler
 *
 *Return: Always 0.
 */

int main(int argc, char *argv[])
{
(void) argc;
printf("%s\n", argv[0]);
return (0);
}
